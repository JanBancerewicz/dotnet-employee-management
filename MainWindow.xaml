<Window x:Class="dotnet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:dotnet"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1200">
    <StackPanel>
        <Menu>
        <MenuItem Header="Application">
            <MenuItem Header="Generate data" Click="Generate_Click"/>
            <MenuItem Header="Version" Click="Version_Click"/>
            <Separator/>
            <MenuItem Header="Exit" Click="Exit_Click"/>
        </MenuItem>

        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*" />
                <ColumnDefinition Width="5" />
                <!-- Separator -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition MinHeight="500" Height="*" />
            </Grid.RowDefinitions>

            <!-- Lewy panel -->
            <StackPanel Grid.Column="0" Margin="0">
                <TextBox Name="textBox1" TextWrapping="Wrap" AcceptsReturn="True" VerticalAlignment="Stretch" />
                <TreeView Name="PracownicyTreeView" Margin="10" Height="445">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Podwladni}">
                            <TextBlock Text="{Binding}" />
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </StackPanel>

            <!-- Separator -->
            <Border Grid.Column="1" Background="Gray" />

            <!-- Prawy panel -->
            <StackPanel Grid.Column="2" Margin="0">
                <TextBox Name="textBox2" TextWrapping="Wrap" AcceptsReturn="True" VerticalAlignment="Stretch" />
            </StackPanel>
        </Grid>


    </StackPanel>
</Window>
